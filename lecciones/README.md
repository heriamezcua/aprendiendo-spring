# Lecciones

Cada carpeta es un proyecto Spring Boot independiente, que se arranca por separado:

```bash
cd lecciones/01-hola-spring
./mvnw spring-boot:run
```

La teoría de cada lección está en https://spring.heriamezcua.es
